import { Metadata } from "next";

export const metadata: Metadata = {
    title: 'Inicio'
}

export default function Home() {
    return (
        <div>
            <h1>Inicio</h1>
        </div>
    )
}
